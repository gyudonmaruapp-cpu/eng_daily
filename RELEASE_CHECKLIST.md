# 公開までのタスクチェックリスト

「英語名言 日めくりカレンダー」をApp Storeに出すまでにやることの全体像。実装済み/未実装・誰がやる作業かを書いてある。チェックが付いていない項目は全部未対応。

凡例：**[要ユーザー]** は開発者本人にしかできない作業（アカウント登録・支払い・実機での確認など）。**[Claude可]** はコードやドキュメントとして自分（Claude）が引き続き手伝える作業。

---

## 1. Apple Developer アカウント周り 【要ユーザー】

- [ ] Apple Developer Program に登録する（年 $99）
- [ ] Team ID を確認する（developer.apple.com または Xcode の Signing & Capabilities）
- [ ] `app.json` の `expo.ios.appleTeamId` に Team ID を追加する（今は未設定。ウィジェットのビルドに必須）
- [ ] `app.json` の `ios.bundleIdentifier` / `android.package` を本番用の値に変更する（今は仮の `com.engdaily.app` のまま）

## 2. アセット（アイコン・スプラッシュ・スクリーンショット）

- [ ] **[要ユーザー or Claude可]** アプリアイコンをデザインする（今は `assets/icon.png` 等、Expo のデフォルトプレースホルダーのまま。Modernist のトンマナに合わせたアイコン案は自分でも作成可能）
- [ ] スプラッシュ画面をデザインする（同上、今はデフォルトのまま）
- [ ] **[要ユーザー]** App Store 掲載用スクリーンショットを撮影する（実機 or シミュレータが必要。iPhone の複数サイズ分が必要な場合あり）
- [ ] （任意）プレビュー動画を用意する

## 3. 広告（AdMob）【要ユーザー】

- [x] `react-native-google-mobile-ads` の組み込み・バナー広告の設置（実装済み、テストID）
- [ ] AdMob アカウントを作成し、アプリを登録してアプリID・広告ユニットIDを取得する
- [ ] `app.json` の `iosAppId`（`react-native-google-mobile-ads` プラグイン設定）を取得したIDに差し替える
- [ ] `src/utils/ads.native.ts` の `BANNER_AD_UNIT_ID` を取得した広告ユニットIDに差し替える
- [ ] 実機で広告が実際に表示されることを確認する（テストIDのままだと収益は発生しない）

## 4. 法務（利用規約・プライバシーポリシー）

- [x] 利用規約・プライバシーポリシーの草案作成（アプリ内画面 + `legal/index.html`）— **未レビューのテンプレート草案**
- [ ] **[要ユーザー]** 内容を確認する（可能なら専門家にレビューしてもらう。特に広告・未成年利用者を含む点）
- [ ] **[要ユーザー]** 連絡先メールアドレスを実際に使うものに更新する（今は `tujuliangtai@gmail.com` のプレースホルダー）
- [ ] **[要ユーザー]** `legal/index.html` を GitHub Pages 等で公開し、URL を確定する
- [ ] 確定した URL を `legal/index.html` 内・App Store Connect の「プライバシーポリシーURL」欄に反映する

## 5. iOS ウィジェット（WidgetKit）

- [x] ウィジェットのコード実装（`targets/widget/`、SwiftUI）
- [ ] **[要ユーザー]** Mac + Xcode、または EAS Build で実際にビルドし、ホーム画面での見た目を初めて確認する（この環境には Xcode がなく一度も見れていない）
- [ ] レイアウト・文字サイズなど、実機で見た結果に応じて調整する（**Claude可**、フィードバックがあれば）

## 6. 実機テスト 【要ユーザー】

現状、動作確認は TypeScript の型チェックと Web バンドルのエクスポートのみ。実機・シミュレータでは一度も動かしていない。

- [ ] Expo Go または development build で全画面を一通り触る（ホーム/アーカイブ/お気に入り/設定/シェア）
- [ ] 通知（時刻設定 → 実際に届くか）を確認する
- [ ] 名言カードの画像保存（写真ライブラリへの書き込み）を確認する
- [ ] シェア機能（X / LINE / Instagram / その他）が実機で正しく起動するか確認する
- [ ] 広告バナーの表示を確認する
- [ ] ウィジェットの動作を確認する（6と重複）
- [ ] 友人・家族など数人に TestFlight で触ってもらう（内部テスト）

## 7. App Store Connect 提出準備 【要ユーザー】

- [ ] アプリ名・サブタイトルを決める
- [ ] 説明文（プロモーション文・詳細説明）を書く（**Claude可**、下書きは手伝える）
- [ ] キーワードを決める
- [ ] カテゴリを選ぶ（教育 / ライフスタイル 等）
- [ ] サポートURL を用意する（問い合わせ先ページ。今は何もない）
- [ ] （任意）マーケティングURL を用意する
- [ ] 年齢レーティング質問票に回答する（Kids Category には申請しない前提）
- [ ] App Privacy（データ収集の申告）に回答する — AdMob が収集するデータの種類を正確に申告する必要がある
- [ ] 価格・対応国・対応言語を設定する（無料想定）

## 8. ビルド・提出

```sh
npm install -g eas-cli
eas login
eas build:configure
eas build -p ios --profile preview     # まずは内部確認用
eas build -p ios --profile production  # 本番提出用
eas submit -p ios
```

- [ ] `eas build:configure` を実行する
- [ ] preview ビルドで動作確認する
- [ ] production ビルドを作成する
- [ ] App Store Connect に提出する（`eas submit`）
- [ ] Apple の審査を通過する（内容によっては差し戻し対応が発生する）

## 9. 公開後（やらなくてもいいが検討事項）

- [ ] Android 対応の検討（企画書どおり、今回のスコープ外。Expo なので同一コードベースでほぼ対応可能）
- [ ] 名言データの追加・見直し（現状365件、月日固定で毎年循環）
- [ ] 学校・塾向けB2Cライセンスなど、企画書にあったマネタイズ案の検証
- [ ] リモートリポジトリ（GitHub等）へのプッシュ（現状ローカルのみ。バックアップ・共同作業のため推奨）

---

## まず何からやるべきか

1. **Apple Developer Program 登録**（時間がかかるので最優先）
2. 登録が進んでいる間に、AdMob アカウント作成・アイコンデザイン・説明文などを並行して進める
3. Team ID が手に入ったら `eas build:configure` → preview ビルド → 実機で一通りテスト
4. 問題なければ本番ビルド → 審査提出
